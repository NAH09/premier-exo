from re import A


nombre= []

class Addition : 
    def __init__(self, nombre) :
        self.nombre= nombre

    def check (self)
        self.vald=True
        for s in self.nombre :
            if not isinstance(nombre, (int, float)) :